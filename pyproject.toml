[project]
name = "comfyui-h3-vision-promptor-planner"
version = "1.1.0"
description = "Fork of ComfyUI-H3-VisionPromptor: local, API-key-free MiniMax H3 prompt enhancer with LongMedia Planner + Cameras outputs (planner_prompt, planner_prompt_ru, global_prompt, clip_durations, camera_params, reference_images_status)."
license = { text = "MIT" }
dependencies = []

[project.urls]
Repository = "https://github.com/your-publisher/ComfyUI-H3-VisionPromptor-Planner"

[tool.comfy]
PublisherId = ""
DisplayName = "H3 Vision Promptor (Planner fork)"
Icon = ""
Web = "web"
